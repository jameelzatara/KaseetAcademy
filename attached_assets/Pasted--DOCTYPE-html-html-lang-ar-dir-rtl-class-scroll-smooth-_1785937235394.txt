<!DOCTYPE html>
<html lang="ar" dir="rtl" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>سمعنا صوتك - استوديو التقييم الصوتي | أكاديمية كاسيت</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Tajawal -->
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800;900&display=swap" rel="stylesheet">
    <!-- Lucide Icons CDN -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        tajawal: ['Tajawal', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            dark: '#0B0F17',
                            card: '#151D2A',
                            gold: '#F59E0B',
                            goldHover: '#D97706',
                            accent: '#FBBF24',
                            border: 'rgba(245, 158, 11, 0.2)'
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #0B0F17;
            color: #F3F4F6;
        }
        .studio-glass {
            background: rgba(21, 29, 42, 0.75);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(245, 158, 11, 0.25);
        }
        .glow-gold {
            box-shadow: 0 0 25px rgba(245, 158, 11, 0.25);
        }
        .glow-btn:hover {
            box-shadow: 0 0 20px rgba(245, 158, 11, 0.5);
        }
        /* Audio Visualizer Wave Lines Extension */
        .wave-line-container {
            display: flex;
            align-items: center;
            width: 100%;
        }
        .wave-line-stroke {
            flex-grow: 1;
            height: 2px;
            background: linear-gradient(90deg, rgba(245,158,11,0) 0%, rgba(245,158,11,0.6) 100%);
        }
        .wave-line-stroke.reverse {
            background: linear-gradient(90deg, rgba(245,158,11,0.6) 0%, rgba(245,158,11,0) 100%);
        }
    </style>
</head>
<body class="bg-brand-dark text-slate-100 min-h-screen flex flex-col justify-between overflow-x-hidden">

    <!-- HEADER / NAVIGATION (طراز المسار الإعلامي) -->
    <header class="fixed top-0 left-0 right-0 z-50 studio-glass border-b border-amber-500/10 px-4 lg:px-8 py-3.5 transition-all">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
            
            <!-- Logo & Brand -->
            <a href="#" class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-xl bg-gradient-to-tr from-amber-600 to-amber-400 p-0.5 flex items-center justify-center shadow-lg shadow-amber-500/20">
                    <div class="w-full h-full bg-slate-950 rounded-[10px] flex items-center justify-center">
                        <i data-lucide="radio" class="w-5 h-5 text-amber-400"></i>
                    </div>
                </div>
                <div class="flex flex-col">
                    <span class="font-extrabold text-xl tracking-tight text-white leading-none">كاسيت</span>
                    <span class="text-[10px] text-amber-400 font-medium tracking-widest mt-1">KASEET ACADEMY</span>
                </div>
            </a>

            <!-- Header Controls -->
            <div class="flex items-center gap-3 lg:gap-4">
                
                <!-- Currency Selector Dropdown -->
                <div class="relative">
                    <button id="currencyBtn" onclick="toggleCurrencyModal()" class="flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-slate-800/80 border border-slate-700 text-xs font-semibold text-slate-200 hover:border-amber-500/50 transition">
                        <i data-lucide="globe" class="w-3.5 h-3.5 text-amber-400"></i>
                        <span id="selectedCurrency">JOD ع.أ</span>
                        <i data-lucide="chevron-down" class="w-3.5 h-3.5 text-slate-400"></i>
                    </button>
                    <!-- Currency Dropdown Menu -->
                    <div id="currencyMenu" class="hidden absolute left-0 mt-2 w-32 studio-glass rounded-xl shadow-2xl overflow-hidden z-50 border border-amber-500/20 py-1">
                        <button onclick="selectCurrency('JOD ع.أ')" class="w-full text-right px-4 py-2 text-xs hover:bg-amber-500/10 hover:text-amber-400 transition">JOD (دينار أردني)</button>
                        <button onclick="selectCurrency('SAR ر.س')" class="w-full text-right px-4 py-2 text-xs hover:bg-amber-500/10 hover:text-amber-400 transition">SAR (ريال سعودي)</button>
                        <button onclick="selectCurrency('AED د.إ')" class="w-full text-right px-4 py-2 text-xs hover:bg-amber-500/10 hover:text-amber-400 transition">AED (درهم إماراتي)</button>
                        <button onclick="selectCurrency('USD $')" class="w-full text-right px-4 py-2 text-xs hover:bg-amber-500/10 hover:text-amber-400 transition">USD (دولار أمريكي)</button>
                    </div>
                </div>

                <!-- CTA Button -->
                <a href="#voice-wizard" class="hidden sm:flex items-center gap-2 bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 text-slate-950 font-bold px-5 py-2 rounded-xl text-sm transition glow-btn shadow-md shadow-amber-500/20">
                    <span>ابدأ رحلتك الصوتية</span>
                    <i data-lucide="mic" class="w-4 h-4"></i>
                </a>

                <!-- Hamburger Menu Button -->
                <button onclick="toggleMobileDrawer()" class="p-2 rounded-xl bg-slate-800/80 border border-slate-700 text-slate-200 hover:text-amber-400 hover:border-amber-500/50 transition">
                    <i data-lucide="menu" class="w-5 h-5"></i>
                </button>
            </div>
        </div>
    </header>

    <!-- MOBILE DRAWER MENU (يحتوي فقط وحصرياً على تسجيل الدخول / حسابي) -->
    <div id="mobileDrawer" class="fixed inset-0 z-50 bg-slate-950/80 backdrop-blur-md hidden transition-all duration-300">
        <div class="fixed top-0 right-0 bottom-0 w-80 studio-glass p-6 border-l border-amber-500/20 flex flex-col justify-between">
            <div>
                <div class="flex items-center justify-between pb-6 border-b border-slate-800">
                    <span class="font-bold text-slate-300 text-sm">القائمة الرئيسية</span>
                    <button onclick="toggleMobileDrawer()" class="p-2 rounded-lg bg-slate-800 text-slate-400 hover:text-white">
                        <i data-lucide="x" class="w-5 h-5"></i>
                    </button>
                </div>
                <div class="mt-8">
                    <a href="#" class="flex items-center gap-3 px-4 py-3.5 rounded-xl bg-gradient-to-r from-amber-500/10 to-transparent border border-amber-500/30 text-amber-400 font-bold hover:bg-amber-500 hover:text-slate-950 transition">
                        <i data-lucide="user" class="w-5 h-5"></i>
                        <span>تسجيل الدخول / حسابي</span>
                    </a>
                </div>
            </div>
            <div class="text-center text-xs text-slate-500 border-t border-slate-800 pt-4">
                جميع الحقوق محفوظة © أكاديمية كاسيت
            </div>
        </div>
    </div>

    <!-- HERO SECTION -->
    <section class="relative pt-32 pb-20 md:pt-40 md:pb-28 overflow-hidden">
        <!-- Background Ambient Grid & Glow -->
        <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-amber-500/10 via-slate-950 to-slate-950 -z-10"></div>
        <div class="absolute top-1/4 right-1/2 translate-x-1/2 w-[500px] h-[500px] bg-amber-500/10 blur-[140px] rounded-full pointer-events-none -z-10"></div>

        <div class="max-w-5xl mx-auto px-4 text-center">
            
            <!-- Badge -->
            <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full studio-glass border-amber-500/30 text-amber-400 text-xs font-bold mb-6">
                <i data-lucide="sparkles" class="w-4 h-4"></i>
                <span>استوديو التقييم الصوتي الاحترافي</span>
            </div>

            <!-- Main Title -->
            <h1 class="text-3xl md:text-5xl lg:text-6xl font-black text-white leading-tight md:leading-tight mb-6 tracking-tight">
                اخفض صوت العالم... <br>
                <span class="bg-gradient-to-r from-amber-300 via-amber-400 to-amber-500 bg-clip-text text-transparent">وارفع صوت الكاسيت</span>
            </h1>

            <!-- Subtitle -->
            <p class="text-slate-400 text-base md:text-xl max-w-2xl mx-auto mb-10 leading-relaxed font-normal">
                لكل صَوت حكاية... قس قدراتك الصوتية واستلم تقريراً استشارياً معتمداً ومباشراً من خبراء كاسيت.
            </p>

            <!-- Explore Button -->
            <div class="flex flex-col items-center gap-3">
                <a href="#voice-wizard" class="inline-flex items-center gap-2 bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 text-slate-950 font-black px-8 py-4 rounded-2xl text-base transition shadow-xl shadow-amber-500/20 glow-btn">
                    <span>استكشف الاستوديو التفاعلي</span>
                    <i data-lucide="chevron-down" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- STATS BAR (Glassmorphism & Smooth Lag-free Counters) -->
            <div class="mt-16 md:mt-24 max-w-4xl mx-auto">
                <div class="studio-glass rounded-2xl p-6 md:p-8 shadow-2xl border border-amber-500/20">
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 divide-y md:divide-y-0 md:divide-x md:divide-x-reverse divide-slate-800">
                        
                        <!-- Stat 1 -->
                        <div class="flex flex-col items-center pt-4 md:pt-0">
                            <div class="w-12 h-12 rounded-xl bg-amber-500/10 border border-amber-500/20 flex items-center justify-center text-amber-400 mb-3">
                                <i data-lucide="users" class="w-6 h-6"></i>
                            </div>
                            <span class="text-3xl md:text-4xl font-extrabold text-white font-mono tracking-tight" id="counter1" data-target="5000">0</span>
                            <span class="text-xs text-slate-400 font-medium mt-1">+ طالب مسجل</span>
                        </div>

                        <!-- Stat 2 -->
                        <div class="flex flex-col items-center pt-4 md:pt-0">
                            <div class="w-12 h-12 rounded-xl bg-amber-500/10 border border-amber-500/20 flex items-center justify-center text-amber-400 mb-3">
                                <i data-lucide="award" class="w-6 h-6"></i>
                            </div>
                            <span class="text-3xl md:text-4xl font-extrabold text-white font-mono tracking-tight" id="counter2" data-target="700">0</span>
                            <span class="text-xs text-slate-400 font-medium mt-1">+ دورة تدريبية</span>
                        </div>

                        <!-- Stat 3 -->
                        <div class="flex flex-col items-center pt-4 md:pt-0">
                            <div class="w-12 h-12 rounded-xl bg-amber-500/10 border border-amber-500/20 flex items-center justify-center text-amber-400 mb-3">
                                <i data-lucide="clock" class="w-6 h-6"></i>
                            </div>
                            <span class="text-3xl md:text-4xl font-extrabold text-white font-mono tracking-tight" id="counter3" data-target="8500">0</span>
                            <span class="text-xs text-slate-400 font-medium mt-1">+ ساعة تدريب مباشر</span>
                        </div>

                    </div>
                </div>

                <!-- Complete Full-Width Wave Extension Bar -->
                <div class="wave-line-container mt-6 px-4">
                    <div class="wave-line-stroke"></div>
                    <div class="px-4 text-amber-500/50 flex items-center gap-1.5 text-[11px] font-mono tracking-widest">
                        <i data-lucide="activity" class="w-4 h-4 text-amber-400 animate-pulse"></i>
                        <span>AUDIO WAVEFORM SPECTRUM</span>
                    </div>
                    <div class="wave-line-stroke reverse"></div>
                </div>
            </div>

        </div>
    </section>

    <!-- VOICE STUDIO WIZARD (REALISTIC DAW CONSOLE EXPERIENCE) -->
    <section id="voice-wizard" class="py-16 md:py-24 relative">
        <div class="max-w-4xl mx-auto px-4">
            
            <div class="text-center mb-10">
                <h2 class="text-2xl md:text-4xl font-black text-white mb-3">استوديو سمعنا صوتك التفاعلي</h2>
                <p class="text-slate-400 text-sm md:text-base">سجّل عينتك الصوتية في 30 ثانية وسيقوم خبراء كاسيت بتحليل خامة صوتك</p>
            </div>

            <!-- Studio Console Card -->
            <div class="studio-glass rounded-3xl p-6 md:p-10 shadow-2xl border border-amber-500/30 relative overflow-hidden glow-gold">
                
                <!-- Wizard Progress Steps Header -->
                <div class="flex items-center justify-between mb-8 pb-6 border-b border-slate-800/80">
                    <div id="stepHeader1" class="flex items-center gap-2 text-amber-400 font-bold text-sm">
                        <span class="w-7 h-7 rounded-full bg-amber-500 text-slate-950 flex items-center justify-center text-xs font-black">1</span>
                        <span>تحديد المسار</span>
                    </div>
                    <div class="w-12 h-0.5 bg-slate-800"></div>
                    <div id="stepHeader2" class="flex items-center gap-2 text-slate-500 font-bold text-sm">
                        <span class="w-7 h-7 rounded-full bg-slate-800 text-slate-400 flex items-center justify-center text-xs font-black">2</span>
                        <span>تسجيل الصوت</span>
                    </div>
                    <div class="w-12 h-0.5 bg-slate-800"></div>
                    <div id="stepHeader3" class="flex items-center gap-2 text-slate-500 font-bold text-sm">
                        <span class="w-7 h-7 rounded-full bg-slate-800 text-slate-400 flex items-center justify-center text-xs font-black">3</span>
                        <span>إرسال التقرير</span>
                    </div>
                </div>

                <!-- STEP 1: TRACK & LEVEL SELECTION -->
                <div id="step1" class="space-y-6">
                    <div>
                        <label class="block text-xs font-bold text-amber-400 mb-3 uppercase tracking-wider">اختر المجال الصوتي المستهدف</label>
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                            <button onclick="selectCategory(this, 'إعلانات وتجاري')" class="cat-btn studio-glass p-4 rounded-2xl border-slate-800 hover:border-amber-500/50 flex flex-col items-center gap-2 text-center transition">
                                <i data-lucide="megaphone" class="w-6 h-6 text-amber-400"></i>
                                <span class="text-xs font-bold text-slate-200">الإعلانات والترويج</span>
                            </button>
                            <button onclick="selectCategory(this, 'وثائقيات وسرد')" class="cat-btn studio-glass p-4 rounded-2xl border-slate-800 hover:border-amber-500/50 flex flex-col items-center gap-2 text-center transition">
                                <i data-lucide="book-open" class="w-6 h-6 text-amber-400"></i>
                                <span class="text-xs font-bold text-slate-200">الوثائقيات والسرد</span>
                            </button>
                            <button onclick="selectCategory(this, 'دبلجة وتمثيل صوتي')" class="cat-btn studio-glass p-4 rounded-2xl border-slate-800 hover:border-amber-500/50 flex flex-col items-center gap-2 text-center transition">
                                <i data-lucide="clapperboard" class="w-6 h-6 text-amber-400"></i>
                                <span class="text-xs font-bold text-slate-200">الدبلجة والتمثيل</span>
                            </button>
                            <button onclick="selectCategory(this, 'رد آلي IVR')" class="cat-btn studio-glass p-4 rounded-2xl border-slate-800 hover:border-amber-500/50 flex flex-col items-center gap-2 text-center transition">
                                <i data-lucide="phone-call" class="w-6 h-6 text-amber-400"></i>
                                <span class="text-xs font-bold text-slate-200">الرد الآلي IVR</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-bold text-amber-400 mb-3 uppercase tracking-wider">مستوى خبرتك الحالية</label>
                        <select id="userLevel" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none focus:border-amber-500 transition">
                            <option value="مبتدئ شغوف">مبتدئ شغوف (لم أقم بالتدريب سابقاً)</option>
                            <option value="متوسط">متوسط (لدي محاولات تسجيل سابقة)</option>
                            <option value="متقدم / محترف">متقدم / محترف (أبحث عن تطوير المهارة)</option>
                        </select>
                    </div>

                    <button onclick="goToStep(2)" class="w-full bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 text-slate-950 font-black py-4 rounded-xl text-base transition shadow-lg glow-btn flex items-center justify-center gap-2">
                        <span>انتقل إلى الاستوديو للتسجيل</span>
                        <i data-lucide="arrow-left" class="w-5 h-5"></i>
                    </button>
                </div>

                <!-- STEP 2: REALTIME AUDIO RECORDER & VISUALIZER -->
                <div id="step2" class="hidden space-y-6 text-center">
                    
                    <!-- Script Prompt text to read -->
                    <div class="bg-slate-900/90 border border-amber-500/20 p-4 rounded-2xl text-right">
                        <span class="text-[10px] text-amber-400 font-bold block mb-1">النص المقترح للتسجيل:</span>
                        <p class="text-sm text-slate-200 leading-relaxed">"في عالم يتسارع بالضوضاء، يبقى الصوت الرخيم هو البصمة التي لا تُنسى. أكاديمية كاسيت... حيث يبدأ شغفك الصوتي."</p>
                    </div>

                    <!-- Audio Visualizer Canvas -->
                    <div class="relative bg-slate-950 border border-slate-800 rounded-2xl p-4 h-32 flex items-center justify-center overflow-hidden">
                        <canvas id="visualizerCanvas" class="w-full h-full"></canvas>
                        <div id="recordTimer" class="absolute top-3 left-3 font-mono text-sm text-amber-400 font-bold bg-slate-900/80 px-3 py-1 rounded-md border border-amber-500/20">
                            00:30
                        </div>
                    </div>

                    <!-- Mic Action Buttons -->
                    <div class="flex items-center justify-center gap-4">
                        <button id="startRecBtn" onclick="toggleRecording()" class="w-20 h-20 rounded-full bg-gradient-to-tr from-amber-600 to-amber-400 text-slate-950 flex items-center justify-center shadow-xl shadow-amber-500/30 hover:scale-105 transition">
                            <i data-lucide="mic" class="w-8 h-8"></i>
                        </button>
                    </div>
                    <span id="recStatus" class="text-xs text-slate-400 font-medium block">اضغط على الميكروفون لبدء التسجيل (30 ثانية max)</span>

                    <!-- Preview Audio Player (Appears after recording) -->
                    <div id="audioPreviewContainer" class="hidden bg-slate-900/80 p-4 rounded-2xl border border-slate-800 space-y-3">
                        <audio id="audioPlayback" controls class="w-full"></audio>
                        <div class="flex justify-center gap-3">
                            <button onclick="resetRecording()" class="px-4 py-2 rounded-xl bg-slate-800 text-xs font-bold text-slate-300 hover:bg-slate-700 flex items-center gap-1">
                                <i data-lucide="rotate-ccw" class="w-3.5 h-3.5"></i>
                                <span>إعادة التسجيل</span>
                            </button>
                            <button onclick="goToStep(3)" class="px-6 py-2 rounded-xl bg-amber-500 text-slate-950 text-xs font-black hover:bg-amber-400 flex items-center gap-1">
                                <span>اعتماد عينة الصوت</span>
                                <i data-lucide="check" class="w-3.5 h-3.5"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- STEP 3: CREATIVE LEAD FORM & LIVE STUDIO PASS -->
                <div id="step3" class="hidden space-y-6">
                    
                    <!-- Innovative Live Studio Pass Preview Badge -->
                    <div class="bg-gradient-to-r from-slate-900 via-slate-800 to-slate-900 border border-amber-500/30 rounded-2xl p-4 flex items-center justify-between">
                        <div class="flex items-center gap-3">
                            <div class="w-12 h-12 rounded-full bg-amber-500/20 border border-amber-500/40 flex items-center justify-center text-amber-400 font-black">
                                <i data-lucide="headphones" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <span class="text-[10px] text-amber-400 font-mono tracking-widest block">STUDIO EVALUATION PASS</span>
                                <h4 id="passName" class="font-extrabold text-white text-sm">اسم المشترك</h4>
                                <span id="passCountry" class="text-xs text-slate-400">الدولة: غير محددة</span>
                            </div>
                        </div>
                        <div class="text-left">
                            <span class="inline-block px-2.5 py-1 rounded-md bg-amber-500/10 border border-amber-500/30 text-[10px] text-amber-400 font-bold">عينة جاهزة للتحليل</span>
                        </div>
                    </div>

                    <!-- Lead Form Fields -->
                    <form id="leadForm" onsubmit="submitStudioData(event)" class="space-y-4">
                        <div>
                            <label class="block text-xs font-bold text-slate-300 mb-1.5">الاسم الكامل</label>
                            <div class="relative">
                                <input type="text" id="fullName" required oninput="updatePass()" placeholder="أدخل اسمك الثلاثي" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-100 pl-10 focus:outline-none focus:border-amber-500 transition">
                                <i data-lucide="user" class="w-4 h-4 text-slate-500 absolute left-3 top-3.5"></i>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold text-slate-300 mb-1.5">رقم الواتساب (مع الرمز الدولي)</label>
                                <div class="relative">
                                    <input type="tel" id="whatsapp" required placeholder="+962 7X XXX XXXX" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-100 pl-10 focus:outline-none focus:border-amber-500 transition">
                                    <i data-lucide="phone-call" class="w-4 h-4 text-slate-500 absolute left-3 top-3.5"></i>
                                </div>
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-slate-300 mb-1.5">البريد الإلكتروني</label>
                                <div class="relative">
                                    <input type="email" id="email" required placeholder="name@example.com" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-100 pl-10 focus:outline-none focus:border-amber-500 transition">
                                    <i data-lucide="mail" class="w-4 h-4 text-slate-500 absolute left-3 top-3.5"></i>
                                </div>
                            </div>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-300 mb-1.5">الدولة</label>
                            <div class="relative">
                                <input type="text" id="country" required oninput="updatePass()" placeholder="مثال: الأردن، السعودية، الإمارات..." class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-100 pl-10 focus:outline-none focus:border-amber-500 transition">
                                <i data-lucide="globe" class="w-4 h-4 text-slate-500 absolute left-3 top-3.5"></i>
                            </div>
                        </div>

                        <button type="submit" id="submitBtn" class="w-full bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 text-slate-950 font-black py-4 rounded-xl text-base transition shadow-xl glow-btn flex items-center justify-center gap-2 mt-6">
                            <span>إرسال العينة الصوتية للتقييم المباشر</span>
                            <i data-lucide="send" class="w-5 h-5"></i>
                        </button>
                    </form>
                </div>

                <!-- SUCCESS SCREEN -->
                <div id="successScreen" class="hidden text-center py-12 space-y-4">
                    <div class="w-20 h-20 rounded-full bg-amber-500/20 border border-amber-500/40 text-amber-400 flex items-center justify-center mx-auto mb-4">
                        <i data-lucide="check-circle-2" class="w-10 h-10"></i>
                    </div>
                    <h3 class="text-2xl font-black text-white">تم استلام عينتك الصوتية بنجاح!</h3>
                    <p class="text-slate-400 text-sm max-w-md mx-auto">يقوم فريق الخبراء الآن بمراجعة التسجيل الصوتي، وسيتم إرسال تقرير التقييم والتوصيات مباشرة عبر الواتساب خلال 48 ساعة.</p>
                </div>

            </div>
        </div>
    </section>

    <!-- EVALUATORS SECTION -->
    <section class="py-16 bg-slate-950/60 border-t border-slate-800/80">
        <div class="max-w-5xl mx-auto px-4">
            <div class="text-center mb-12">
                <h3 class="text-xl md:text-3xl font-extrabold text-white mb-2">خبراء التقييم والاستشارات</h3>
                <p class="text-slate-400 text-xs md:text-sm">يتم تقييم صوتك بواسطة مدربين محترفين في الهندسة والأداء الصوتي</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-3xl mx-auto">
                
                <!-- Evaluator 1 -->
                <div class="studio-glass p-6 rounded-2xl border-slate-800 flex items-center gap-4">
                    <div class="w-14 h-14 rounded-full bg-amber-500/10 border border-amber-500/30 flex items-center justify-center text-amber-400 shrink-0">
                        <i data-lucide="mic" class="w-7 h-7"></i>
                    </div>
                    <div>
                        <h4 class="font-bold text-white text-base">خبير الهندسة والفوكال</h4>
                        <p class="text-xs text-amber-400 font-medium mt-0.5">تحليل الطبقات، التنفس، والنبرة</p>
                        <p class="text-[11px] text-slate-400 mt-2">قياس المساحات الصوتية ونقاء مخارج الحروف</p>
                    </div>
                </div>

                <!-- Evaluator 2 -->
                <div class="studio-glass p-6 rounded-2xl border-slate-800 flex items-center gap-4">
                    <div class="w-14 h-14 rounded-full bg-amber-500/10 border border-amber-500/30 flex items-center justify-center text-amber-400 shrink-0">
                        <i data-lucide="headphones" class="w-7 h-7"></i>
                    </div>
                    <div>
                        <h4 class="font-bold text-white text-base">خبير الأداء والإلقاء</h4>
                        <p class="text-xs text-amber-400 font-medium mt-0.5">تحليل التلوين الصوتي والتعبير</p>
                        <p class="text-[11px] text-slate-400 mt-2">تقييم الملاءمة للنصوص الوثائقية والإعلانية</p>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer class="studio-glass border-t border-slate-800/80 py-8 text-center text-xs text-slate-500">
        <div class="max-w-7xl mx-auto px-4 flex flex-col md:flex-row items-center justify-between gap-4">
            <span>جميع الحقوق محفوظة © أكاديمية كاسيت 2026</span>
            <div class="flex items-center gap-6 text-slate-400">
                <a href="#" class="hover:text-amber-400 transition">الشروط والأحكام</a>
                <a href="#" class="hover:text-amber-400 transition">سياسة الخصوصية</a>
            </div>
        </div>
    </footer>

    <!-- LOGIC SCRIPT -->
    <script>
        // Initialize Lucide Icons
        lucide.createIcons();

        // Mobile Drawer Toggle
        function toggleMobileDrawer() {
            const drawer = document.getElementById('mobileDrawer');
            drawer.classList.toggle('hidden');
        }

        // Currency Modal Toggle
        function toggleCurrencyModal() {
            const menu = document.getElementById('currencyMenu');
            menu.classList.toggle('hidden');
        }

        function selectCurrency(curr) {
            document.getElementById('selectedCurrency').innerText = curr;
            toggleCurrencyModal();
        }

        // Lag-free Counter Animation (requestAnimationFrame)
        function animateCounters() {
            const counters = [
                { id: 'counter1', target: 5000 },
                { id: 'counter2', target: 700 },
                { id: 'counter3', target: 8500 }
            ];

            counters.forEach(c => {
                const el = document.getElementById(c.id);
                let start = 0;
                const duration = 2000;
                const startTime = performance.now();

                function updateCounter(currentTime) {
                    const elapsed = currentTime - startTime;
                    const progress = Math.min(elapsed / duration, 1);
                    const currentVal = Math.floor(progress * c.target);
                    el.innerText = currentVal.toLocaleString();

                    if (progress < 1) {
                        requestAnimationFrame(updateCounter);
                    } else {
                        el.innerText = c.target.toLocaleString();
                    }
                }
                requestAnimationFrame(updateCounter);
            });
        }

        // Intersection Observer for lag-free counter trigger
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animateCounters();
                    observer.disconnect();
                }
            });
        }, { threshold: 0.3 });

        observer.observe(document.getElementById('counter1'));

        // Wizard Logic & Selection
        let selectedCategory = 'إعلانات وتجاري';
        let mediaRecorder;
        let audioChunks = [];
        let recordedBlobBase64 = '';
        let timerInterval;

        function selectCategory(btn, cat) {
            document.querySelectorAll('.cat-btn').forEach(b => b.classList.remove('border-amber-500', 'bg-amber-500/10'));
            btn.classList.add('border-amber-500', 'bg-amber-500/10');
            selectedCategory = cat;
        }

        function goToStep(stepNum) {
            document.getElementById('step1').classList.add('hidden');
            document.getElementById('step2').classList.add('hidden');
            document.getElementById('step3').classList.add('hidden');

            document.getElementById(`step${stepNum}`).classList.remove('hidden');

            // Update Header visual indicators
            for (let i = 1; i <= 3; i++) {
                const h = document.getElementById(`stepHeader${i}`);
                if (i <= stepNum) {
                    h.classList.replace('text-slate-500', 'text-amber-400');
                    h.querySelector('span').classList.replace('bg-slate-800', 'bg-amber-500');
                    h.querySelector('span').classList.replace('text-slate-400', 'text-slate-950');
                }
            }
        }

        // Realtime Studio Audio Visualizer & Recorder Logic
        async function toggleRecording() {
            const btn = document.getElementById('startRecBtn');
            const status = document.getElementById('recStatus');

            if (!mediaRecorder || mediaRecorder.state === 'inactive') {
                try {
                    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                    mediaRecorder = new MediaRecorder(stream);
                    audioChunks = [];

                    // Setup Web Audio API Analyser
                    const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
                    const source = audioCtx.createMediaStreamSource(stream);
                    const analyser = audioCtx.createAnalyser();
                    analyser.fftSize = 64;
                    source.connect(analyser);

                    const canvas = document.getElementById('visualizerCanvas');
                    const canvasCtx = canvas.getContext('2d');
                    const bufferLength = analyser.frequencyBinCount;
                    const dataArray = new Uint8Array(bufferLength);

                    function drawVisualizer() {
                        if (mediaRecorder && mediaRecorder.state === 'recording') {
                            requestAnimationFrame(drawVisualizer);
                            analyser.getByteFrequencyData(dataArray);

                            canvasCtx.fillStyle = '#090D14';
                            canvasCtx.fillRect(0, 0, canvas.width, canvas.height);

                            const barWidth = (canvas.width / bufferLength) * 2.5;
                            let x = 0;

                            for (let i = 0; i < bufferLength; i++) {
                                const barHeight = dataArray[i] / 2;
                                canvasCtx.fillStyle = '#F59E0B';
                                canvasCtx.fillRect(x, canvas.height - barHeight, barWidth, barHeight);
                                x += barWidth + 2;
                            }
                        }
                    }

                    mediaRecorder.ondataavailable = event => audioChunks.push(event.data);
                    mediaRecorder.onstop = async () => {
                        const audioBlob = new Blob(audioChunks, { type: 'audio/webm' });
                        document.getElementById('audioPlayback').src = URL.createObjectURL(audioBlob);
                        document.getElementById('audioPreviewContainer').classList.remove('hidden');

                        // Convert Blob to Base64 for Google Apps Script Webhook
                        const reader = new FileReader();
                        reader.readAsDataURL(audioBlob);
                        reader.onloadend = () => { recordedBlobBase64 = reader.result; };
                    };

                    mediaRecorder.start();
                    drawVisualizer();

                    btn.classList.replace('from-amber-600', 'from-red-600');
                    btn.classList.replace('to-amber-400', 'to-red-500');
                    status.innerText = 'جاري التسجيل الان... اضغط مرة أخرى للإيقاف';

                    // 30 Seconds Countdown
                    let timeLeft = 30;
                    timerInterval = setInterval(() => {
                        timeLeft--;
                        document.getElementById('recordTimer').innerText = `00:${timeLeft < 10 ? '0' : ''}${timeLeft}`;
                        if (timeLeft <= 0) {
                            clearInterval(timerInterval);
                            toggleRecording();
                        }
                    }, 1000);

                } catch (err) {
                    alert('يرجى تفعيل صلاحية استخدام الميكروفون للتسجيل');
                }
            } else {
                mediaRecorder.stop();
                clearInterval(timerInterval);
                btn.classList.replace('from-red-600', 'from-amber-600');
                btn.classList.replace('to-red-500', 'to-amber-400');
                status.innerText = 'تم التسجيل بنجاح، استمع للتسجيل أسفله';
            }
        }

        function resetRecording() {
            document.getElementById('audioPreviewContainer').classList.add('hidden');
            document.getElementById('recordTimer').innerText = '00:30';
            document.getElementById('recStatus').innerText = 'اضغط على الميكروفون لبدء التسجيل (30 ثانية max)';
        }

        // Dynamic Live Pass Update
        function updatePass() {
            const name = document.getElementById('fullName').value;
            const country = document.getElementById('country').value;
            document.getElementById('passName').innerText = name || 'اسم المشترك';
            document.getElementById('passCountry').innerText = `الدولة: ${country || 'غير محددة'}`;
        }

        // Webhook Submission
        async function submitStudioData(e) {
            e.preventDefault();
            const submitBtn = document.getElementById('submitBtn');
            submitBtn.disabled = true;
            submitBtn.innerText = 'جاري إرسال عينة الصوت...';

            const payload = {
                name: document.getElementById('fullName').value,
                whatsapp: document.getElementById('whatsapp').value,
                email: document.getElementById('email').value,
                country: document.getElementById('country').value,
                category: selectedCategory,
                level: document.getElementById('userLevel').value,
                audioData: recordedBlobBase64
            };

            const WEBHOOK_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEBHOOK_URL';

            try {
                if(WEBHOOK_URL !== 'YOUR_GOOGLE_APPS_SCRIPT_WEBHOOK_URL') {
                    await fetch(WEBHOOK_URL, {
                        method: 'POST',
                        mode: 'no-cors',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });
                }
                document.getElementById('step3').classList.add('hidden');
                document.getElementById('successScreen').classList.remove('hidden');
            } catch (err) {
                alert('حدث خطأ أثناء الإرسال، يرجى المحاولة لاحقاً');
                submitBtn.disabled = false;
            }
        }
    </script>
</body>
</html>