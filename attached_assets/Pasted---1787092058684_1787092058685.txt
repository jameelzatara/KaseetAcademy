⛔ هذا تحديث لبناء قائم، لا مشروع جديد. اقرأ القسمين قبل أي تعديل.

═══ القسم ١ — الملفّ المرفق هو المرجع الحرفي ═══

الملفّ "لوحة-إدارة-كاسيت-الكاملة.html" يحتوي على:
  · تصميم اللوحة الكامل (11 قسماً)
  · منطق الصلاحيات الكامل (مدير / مستشارة)
  · محرّك دورة الحياة للدفعات (auto-hide/auto-show)
  · محرّك التفعيل التلقائي للدورات
  · بيانات السبعة برامج الحقيقية (4 دورات نشطة + 3 ماستركلاسات مسوَّدة)

انقل هذا المنطق حرفياً إلى مكوّنات React المرتبطة بقاعدة البيانات الفعلية.
⛔ لا تُعِد تصميم أيّ شاشة ولا تغيّر أيّ قاعدة سلوك موجودة فيه.

═══ القسم ٢ — الفرق الوحيد بين الملفّ وقاعدة بياناتكم ═══

الملفّ يخزّن كل شيء في متغيّرات JavaScript (بلا خادم).
مهمّتك تقتصر على نقل نفس البيانات ونفس القواعد إلى:
  · جدول users (بكلمات مرور مشفَّرة hashed — لا نصّاً صريحاً كما في الملفّ)
  · جدول courses (نفس الحقول: slug, nameAr, level, onsite{}, live{}, capacity, active)
  · جدول cohorts (كما هو معمول به مسبقاً في قاعدتكم)
  · جدول discount_codes
  · جدول subscribers
  · جدول activity_log

⛔ لا تُنشئ حقولاً جديدة غير الموجودة في الملفّ. إن احتجت حقلاً إضافياً، اسأل قبل أن تضيفه.

═══ القرارات الجاهزة — لا تُعاد مناقشتها ═══

١) التفعيل التلقائي للدورة: نشطة فقط عند اكتمال (ساعات + جلسات + سعر) لنمط واحد على الأقلّ.
   لا زرّ "تفعيل" يدوي — الحالة تُحسب من اكتمال البيانات، تماماً كما في دالّة saveCourse() و toggleCourseActive() بالملفّ.

٢) حالة الدفعة (قادمة/متاحة/ممتلئة/منتهية): تُحسب من التاريخ الحالي مقابل start و end.
   ⛔ لا يُخزَّن حقل status ثابت أبداً — انظر دالّة getCohortStatus() بالملفّ، انقلها حرفياً.

٣) الصلاحيات:
   - المستشارة: تُنشئ دورات وتسعّرها بحرّية، لكن لا تعدّل سعر دورة موجودة عليها priceLocked:true
   - لا أحد يحذف كوداً استُخدم ولو مرّة (usedCount > 0) — حتى المدير — يُعطَّل فقط
   - حذف المشتركين وقسم "إدارة الفريق" للمدير فقط

٤) الدورات السبع الحالية بالضبط كما في الملفّ:
   نشطة: أساسيات التعليق (218د.أ/150$) · المذيع المحترف (250د.أ) ·
         تمكين اللغة العربية (150$) · فن الخطابة (180د.أ/150$)
   مسوَّدة بانتظار بيانات: ماستركلاس التعليق · ماستركلاس الإعلام · ماستركلاس الخطابة
   ⛔ لا تخترع أسعاراً أو ساعات لهذه الثلاثة — اتركها فارغة كما في الملفّ حتى تُعبَّأ من اللوحة.

٥) الدفعات الخمس الحالية (137, 138, 201, 202, 203) بمواعيدها الحرفية كما في الملفّ — لا تُعدَّل تواريخها.

═══ الإثبات المطلوب بعد التنفيذ ═══

أرسل فقط:
  ١) لقطة شاشة لقسم "إدارة الدورات" — يجب أن تُظهر السبعة برامج بنفس الترتيب والحالات
  ٢) لقطة لمحاولة مستشارة تعديل سعر "أساسيات التعليق" — الحقل يجب أن يكون معطَّلاً
  ٣) تأكيد أن حذف كود TEST20 (مستخدَم) مرفوض حتى لحساب admin
  ٤) عدد الملفّات/الأسطر التي عدّلتها فعلياً (للتأكّد أنك نقلت لا أعدت البناء)

⛔ لا ترسل "تمّ الإنجاز" بلا الأربعة أعلاه.